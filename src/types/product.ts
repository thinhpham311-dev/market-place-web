
export interface IProduct {
    id: string,
    name?: string,
    article?: string,
    description?: string,
    image?: string,
    price: number,
    discountPrice: number,
    quantity: number,
}